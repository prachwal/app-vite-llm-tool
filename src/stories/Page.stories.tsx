import { Page } from '../components/Page';

export default {
    title: 'Design System/Page',
    component: Page,
    tags: ['autodocs'],
};

const Template = () => <Page />;

export const Default = {
    render: Template,
};
